[README.md](https://github.com/user-attachments/files/25108854/README.md)
# BLAST: Selenium to Playwright Converter (Local LLM)

**BLAST** (Bridging Legacy Automation to Standardized TypeScript) is a local, privacy-focused tool designed to automatically convert legacy **Selenium Java** test code into modern **Playwright TypeScript** with functional parity and best practices.

It leverages a local LLM (**Ollama/CodeLlama**) to perform the conversion, ensuring your code never leaves your machine.

---

## 🏗️ Architecture

```mermaid
graph TD
    User([QA Engineer]) <-->|Interacts via Browser| UI[Web Interface \n(HTML/JS/CSS)]
    UI <-->|POST /api/convert| Backend[Flask Server \n(app.py)]
    Backend <-->|Generate Code| Ollama[Ollama Local API]
    Ollama <-->|Inference| Model[CodeLlama Model]
    
    subgraph "Local Environment"
    UI
    Backend
    Ollama
    Model
    end
```

## ✨ Features

-   **Zero Data Leakage**: Runs 100% locally using Ollama.
-   **Intelligent Conversion**: Converts Java Selenium patterns to Playwright (e.g., `driver.findElement` -> `page.locator`).
-   **Modern Tech Stack**:
    -   **Frontend**: Premium Glassmorphism UI (Vanilla JS + CSS).
    -   **Backend**: Lightweight Python Flask server.
    -   **AI**: CodeLlama via Ollama.
-   **Productivity Tools**: One-click Copy and `.ts` File Download.

## 🚀 Prerequisites

1.  **Python 3.8+** installed.
2.  **Ollama** installed and running. [Download Ollama](https://ollama.com/)
3.  **CodeLlama Model** pulled locally:
    ```bash
    ollama pull codellama
    ```

## 🛠️ Installation & Setup

1.  **Clone the repository** (or download the source):
    ```bash
    git clone <repository-url>
    cd BLAST-selenium-playwright
    ```

2.  **Install Python Dependencies**:
    ```bash
    pip install flask ollama
    ```

3.  **Start the Server**:
    Make sure Ollama is running in the background, then run:
    ```bash
    python app.py
    ```

4.  **Access the Application**:
    Open your browser and navigate to:
    [http://localhost:5000](http://localhost:5000)

## 📖 Usage

1.  Paste your existing **Selenium Java** code into the left editor pane.
2.  Click the **Convert Code** button.
3.  Wait for the local AI to process the translation.
4.  Review the converted **Playwright TypeScript** code on the right.
5.  Use **Copy** or **Download** to save your new test file.

## 📂 Project Structure

```
BLAST/
├── app.py              # Flask Backend & Ollama Integration
├── BLAST.md            # Project Protocol & Roadmap
├── README.md           # Documentation
├── static/
│   ├── css/
│   │   └── style.css   # Premium Styling
│   └── js/
│       └── app.js      # Frontend Logic
└── templates/
    └── index.html      # Main User Interface
```

---
*Generated by Antigravity*
